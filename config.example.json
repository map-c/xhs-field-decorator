{
  "_comment": "本地调试授权 mock：复制为 config.json（已 gitignore，勿提交真值）。dev server（npx dingtalk-docs-cool-app start:field）会读取 authorizations 并在 context.fetch(url, opts, authId) 时注入这些请求头。值取 worker EXTRACT_TOKENS 里 dingtalk 名下的 token。",
  "authorizations": {
    "x-api-token": "<dingtalk-extract-token>"
  }
}
